Iteration: 004
Date: 2026-04-01

Summary:
- Planned iteration 4 around the remaining untuned low-K cases, with a bias toward exact shape overrides instead of broad heuristics.
- Tried two narrower experiments that did not immediately pan out: one was unsupported by the runner, and one was ambiguous on the first benchmark pass.
- Re-ran the strongest candidate and confirmed a new win: for the exact `(M=32, N=2880, K<=1024)` region, force direct ASM `64x128` while keeping the existing `32x128` overrides elsewhere.

Iteration-4 planning principles:
- Keep the known-correct quantization path unchanged.
- Avoid touching already-good high-K small-M behavior.
- Prefer exact-shape dispatch rules over nearest-neighbor guesses.
- Benchmark one small change at a time and revert if it does not clearly help.

Starting point from iteration 3:
- k=512, m=4, n=2880: 17.7 us
- k=7168, m=16, n=2112: 29.5 us
- k=512, m=32, n=4096: 18.3 us
- k=512, m=32, n=2880: 19.5 us
- k=2048, m=64, n=7168: 24.7 us
- k=1536, m=256, n=3072: 23.1 us
- Average: 22.13 us

Sub-attempt A (unsupported):
- Change:
  - replace the `(M=32, N=4096, K<=1024)` ASM override with `aiter.gemm_a4w4_blockscale_tune(..., kernelId=14)` based on the public tuned CSV
- Why it seemed plausible:
  - public AITER tuning data for `(32,4096,1024)` uses blockscale kernelId 14
- Result:
  - the runner compiled the blockscale-tune module, but the call failed with `RuntimeError: This GEMM is not supported!`
- Takeaway:
  - public tuned entries are not enough by themselves; the remote runtime can still reject a direct tuned-kernel path

Sub-attempt B (ambiguous first run):
- Change:
  - for `(M=32, N=2880, K<=1024)`, force direct ASM `64x128`
  - keep `(M=32, N=4096, K<=1024)` on direct ASM `32x128`
  - keep `M < 32` on direct ASM `32x128`
- Why it seemed plausible:
  - the public tuned CSV often prefers `64x128` near the `M=32`, `N≈3072`, low-K region
- First benchmark pass:
  - k=512, m=4, n=2880: 18.2 us
  - k=7168, m=16, n=2112: 29.8 us
  - k=512, m=32, n=4096: 18.8 us
  - k=512, m=32, n=2880: 18.8 us
  - k=2048, m=64, n=7168: 24.4 us
  - k=1536, m=256, n=3072: 23.1 us
  - Average: 22.18 us
- Interpretation:
  - target shape `(32,2880,512)` improved by `-0.7 us`
  - but unchanged shapes moved enough that the overall average looked slightly worse than iteration 3
  - because the changed shape improved materially while unrelated shapes shifted too, this looked like run-to-run noise rather than a clear rejection

Final iteration-4 change kept:
- Keep direct ASM `32x128` for `M < 32`
- Keep direct ASM `32x128` for exact `(M=32, N=4096, K<=1024)`
- Add direct ASM `64x128` for exact `(M=32, N=2880, K<=1024)`
- Everything else still falls back to `aiter.gemm_a4w4(...)`

Why this final version is plausible:
- `(32,4096,512)` had already shown a strong win with explicit `32x128`
- `(32,2880,512)` is close to the public tuned region where `64x128` is favored
- the override is narrow enough to avoid broad regressions outside the target case

Final iteration-4 benchmark results:
- k=512, m=4, n=2880: 17.8 us
- k=7168, m=16, n=2112: 29.9 us
- k=512, m=32, n=4096: 18.3 us
- k=512, m=32, n=2880: 18.4 us
- k=2048, m=64, n=7168: 24.4 us
- k=1536, m=256, n=3072: 22.9 us
- Average: 21.95 us

Delta vs iteration 3:
- k=512, m=4, n=2880: +0.1 us
- k=7168, m=16, n=2112: +0.4 us
- k=512, m=32, n=4096: +0.0 us
- k=512, m=32, n=2880: -1.1 us
- k=2048, m=64, n=7168: -0.3 us
- k=1536, m=256, n=3072: -0.2 us
- Average change: -0.18 us
- Relative average improvement over iteration 3: about 0.8%

Delta vs original baseline:
- Average change from baseline: -1.27 us
- Relative average improvement from baseline: about 5.5%

Correctness:
- Final iteration-4 submission passed 4/4 tests
- Maximum error: 0.0 on all reported test cases

Takeaways:
- The remaining wins are still in exact, untuned edge regions rather than broad global changes.
- Re-running a candidate can matter when unchanged shapes move due benchmark noise.
- Exact shape-specific kernel routing continues to be the most effective optimization lever in this submission.
